/* README instructions */

/*
1. Create project folder and add files above with same paths.
2. Run `npm install`.
3. Ensure backend API is running at VITE_API_URL or default http://localhost:5000/api
4. Run `npm run dev` to start Vite dev server.

This scaffold provides:
- Login with token stored in localStorage
- Protected routes and layout
- Context API for Products & Categories
- Tailwind-only styling
- CRUD pages with modal forms

Customize components and styles as needed.
*/